{
  "name": "mks-kanban",
  "version": "0.1.0",
  "private": true,
  "description": "MKS Kanban — standalone web + desktop. One process serves everything.",
  "scripts": {
    "install:all": "npm --prefix backend install && npm --prefix frontend install && npm --prefix desktop install",
    "build:frontend": "npm --prefix frontend run build",
    "build:backend": "npm --prefix backend run build",
    "build": "npm run build:frontend && npm run build:backend",
    "start": "npm --prefix backend run start:prod",
    "dev:backend": "npm --prefix backend run start:dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "lint": "npm --prefix backend run lint && npm --prefix frontend run lint"
  }
}
